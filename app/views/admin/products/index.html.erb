<div class="row">
  <div class="col-md-12">
    <ul>
      <% @products.each do |product| %>
        <li><%= link_to(product.title, admin_product_path(product)) %></li>
      <% end %>
    </ul>
  </div>
</div>
